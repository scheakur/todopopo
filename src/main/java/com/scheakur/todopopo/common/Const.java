package com.scheakur.todopopo.common;

import java.time.ZoneOffset;
import java.time.ZonedDateTime;

public class Const {

	public static final ZonedDateTime EMPTY_DATE = ZonedDateTime.of(0, 1, 1, 0, 0, 0, 0, ZoneOffset.UTC.normalized());

}
